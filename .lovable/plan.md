Plan to fix the AI Listing flow:\n\n1. Remove the double-blink flow\n- Stop opening `UnifiedListingForm` after AI review.\n- Keep the user inside one AI Listing modal from photo upload to processing to completion.\n- Remove the extra review/deploy handoff that currently causes the second modal/page blink.\n\n2. Make voice/text automatically create the listing\n- Keep one primary action after the user records or types: create the listing.\n- The AI function will refine the description and extract fields in the same request, so there is no separate magic/refine button.\n- Update microphone instructions to say: tap mic, describe the listing, tap again, then Swipess creates the listing from your words and photo.\n\n3. Save directly after AI extraction\n- Reuse the listing save logic pattern from `UnifiedListingForm`: upload photo, build listing payload, insert into `listings`, update listing caches.\n- Save fields from AI output plus category, price, city, beds/baths, amenities, vehicle/job fields where available.\n- Require one photo and a signed-in owner before publishing.\n\n4. Redirect when finished\n- After the listing is created, close the AI modal and redirect to `/listing/{newListingId}` so the user immediately sees the created listing.\n- If saving fails, stay on the details step with a clear error instead of bouncing screens.\n\n5. Add clear progress UI\n- Replace the current abstract processing screen with a